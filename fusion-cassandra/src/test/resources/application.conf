fusion.data.cassandra {
  default {
    basic {
      contact-points = ["114.67.96.96:9042"]
      session-keyspace = "watch_log_dev"
      load-balancing-policy.local-datacenter = dc1
    }
    advanced.auth-provider {
      class = PlainTextAuthProvider
      username = devuser
      password = devpass.2019
    }
  }
}
