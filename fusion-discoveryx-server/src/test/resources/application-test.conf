akka.grpc.client {
  "fusion.discoveryx.grpc.NamingService" {
    use-tls = false
    host = localhost
    port = 48000
  }
  "greeter.GreeterService" {
    host = "localhost"
    port = 8001
    use-tls = false
  }
}