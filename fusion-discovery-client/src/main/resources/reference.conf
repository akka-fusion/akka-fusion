fusion.discovery {
  enable = false
  method = "nacos"
  nacos {
    autoRegisterInstance = false
    serviceName = ${fusion.name}
  }
  http-client {
    default {
      queue-buffer-size = 512
    }
  }
}