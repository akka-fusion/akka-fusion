fusion.data {
  kafka {
    producer {
      kafka-clients {
        bootstrap.servers = "localhost:9092"
      }
    }
    consumer {
      kafka-clients {
        bootstrap.servers = "localhost:9092"
      }
    }
  }
}