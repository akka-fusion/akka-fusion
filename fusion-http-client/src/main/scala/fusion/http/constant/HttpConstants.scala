package fusion.http.constant

object HttpConstants {
  val PATH_ROOT        = "fusion.http"
  val PATH_DEFAULT     = s"$PATH_ROOT.default"
  val SERVER_HOST_PATH = s"$PATH_DEFAULT.server.host"
  val SERVER_PORT_PATH = s"$PATH_DEFAULT.server.port"
}
